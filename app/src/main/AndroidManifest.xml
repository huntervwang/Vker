<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.weimob.vker" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.write_external_storage"/>

    <application
        android:name=".application.VkerApplication_"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.weimob.vker.VkerEntryActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".user.LoginActivity_"></activity>
        <activity android:name=".user.RegisterActivity_"></activity>


        <activity android:name=".TestActivity_"/>
        <activity android:name=".MainActivity_"/>
    </application>

</manifest>
